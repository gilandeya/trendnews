name: 1️⃣ جمع الأخبار وتوليد المسودات

on:
  schedule:
    # كل 4 ساعات بتوقيت إسطنبول (UTC+3):
    #   06:17 · 10:17 · 14:17 · 18:17 · 22:17 محليًا
    #   03:17 · 07:17 · 11:17 · 15:17 · 19:17 بتوقيت UTC
    # الدقيقة 17 لا 00: رأس الساعة مزدحم بملايين المهام المجدولة لدى
    # GitHub، والتأخير فيه أطول بكثير.
    - cron: "17 3,7,11,15,19 * * *"
  workflow_dispatch:
    inputs:
      limit:
        description: "عدد المسودات المطلوبة"
        required: false
        default: "5"
      check_sources:
        description: "فحص المصادر فقط بلا توليد مسودات"
        type: boolean
        required: false
        default: false

permissions:
  contents: write
  issues: write

concurrency:
  group: trendnews-collect
  cancel-in-progress: false

jobs:
  collect:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: سحب المستودع
        uses: actions/checkout@v4

      - name: تهيئة بايثون
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: تثبيت المكتبات
        run: pip install -r requirements.txt

      - name: التأكد من الخطوط العربية
        run: bash scripts/fetch_fonts.sh

      - name: فحص المصادر
        if: inputs.check_sources == true
        run: python -m src.check_sources

      - name: توليد المسودات
        if: inputs.check_sources != true
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: python -m src.collect --limit "${{ inputs.limit || '5' }}"

      - name: رفع المسودات إلى المستودع
        id: commit
        run: |
          git config user.name  "trendnews-bot"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          mkdir -p drafts state
          git add -A drafts state
          if git diff --quiet --cached; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
            echo "لا مسودات جديدة"
          else
            git commit -m "chore(drafts): مسودات $(date -u '+%Y-%m-%d %H:%M') UTC"
            git pull --rebase --autostash origin "${GITHUB_REF_NAME}"
            git push origin "HEAD:${GITHUB_REF_NAME}"
            echo "changed=true" >> "$GITHUB_OUTPUT"
          fi

      - name: نسخة احتياطية من الصور (تُنزّل إن كان المستودع خاصًا)
        if: steps.commit.outputs.changed == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: drafts-${{ github.run_id }}
          path: drafts/
          retention-days: 14
          if-no-files-found: ignore

      - name: فتح Issue للمراجعة
        if: steps.commit.outputs.changed == 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python -m src.open_review

      - name: تثبيت رقم الـ Issue في المسودات
        if: steps.commit.outputs.changed == 'true'
        run: |
          mkdir -p drafts
          git add -A drafts
          if ! git diff --quiet --cached; then
            git commit -m "chore(drafts): ربط المسودات بـ Issue المراجعة"
            git pull --rebase --autostash origin "${GITHUB_REF_NAME}"
            git push origin "HEAD:${GITHUB_REF_NAME}"
          fi
