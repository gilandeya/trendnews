# 📰 بوت الأخبار الرائجة → فيسبوك

يستكشف الأخبار الرائجة عالميًا من ٩٤ خلاصة RSS، يصوغها بالعربية عبر Claude API، يبني
لها صورة منشور، ثم يعرضها عليك للمراجعة عبر GitHub Issue — وينشر ما توافق عليه فقط
على صفحة فيسبوك. إلى جانب المسار الرئيسي هذا توجد أربعة مسارات إضافية لتوليد
المحتوى (رادار للعاجل، طلب بكلمات، تحقق من مقال ملصق، مقال من موجز تحريري) تتشارك
كلها نفس نقطة النهاية: مسودة في `drafts/` + Issue للمراجعة + وسم `approved`.

يعمل بالكامل على **GitHub Actions** المجاني، بلا سيرفر وبلا تكلفة استضافة (باستثناء
استدعاءات Claude API نفسها).

---

## كيف يعمل؟

خمسة مسارات مستقلة تولّد محتوى، تتقاطع كلها عند نفس نقطة المراجعة:

```
   ┌─ 1) الجمع (تلقائي عدة مرات يوميًا عبر cron-job.org خارجي) ──────────┐
   │  ٩٤ خلاصة RSS → دمج المكرر (جاكارد + دمج دلالي عبر لغات) →         │
   │  ترتيب بمؤشر الترند → فلترة الذاكرة (٥ أيام) → فرز رخيص (Haiku)   │
   │       ↓                                                             │
   │  preselect.enabled=true (الافتراضي): يتوقف هنا — Issue اختيار      │
   │  بعناوين فقط، تختار أنت ماذا يُصاغ قبل إنفاق أي تكلفة صياغة/صورة    │
   │       ↓ (بعد اختيارك ووسم approved)                                 │
   │  صياغة عربية (Claude) → صورة الخبر + إطار → drafts/ + Issue مراجعة │
   └───────────────────────────────────────────────────────────────────┘

   ┌─ 2) رادار العاجل (كل ~١٥ دقيقة، بلا نموذج إلا عند تجاوز عتبات) ───┐
   │  تسارع تغطية المصادر (بلا LLM) → عتبات صارمة → فرز رخيص →         │
   │  صياغة وصورة → قد يُنشر تلقائيًا بلا مراجعة إن تحققت عتبات أشد     │
   │  (سقف يومي، مصادر مستقلة كافية، ليس مصدرًا رسميًا وحيدًا، تأكد أنه  │
   │  ليس تحديثًا لخبر سابق) — وإلا يدخل المراجعة العادية               │
   └───────────────────────────────────────────────────────────────────┘

   ┌─ 3) طلب بكلمات (Issue بوسم طلب، أو تشغيل يدوي) ────────────────────┐
   │  بحث Google News (عربي+إنجليزي) → أفضل تطابق حرفي مع كلماتك →      │
   │  صياغة وصورة → drafts/ + Issue مراجعة                              │
   └───────────────────────────────────────────────────────────────────┘

   ┌─ 4) تحقق من مقال ملصق (Issue بوسم تحقق) ───────────────────────────┐
   │  استخراج وقائع/أسئلة المقال → بحث مستقل لكل واقعة (لا يُصدَّق نص     │
   │  المقال نفسه) → تصنيف الحكم (مؤكَّدة/شبه مؤكَّدة/مصدر واحد/تخالفها)  │
   │  → تقرير Issue → إن كفى المؤكَّد: صياغة من الوقائع المؤكَّدة فقط    │
   │  (لا من نص المقال إطلاقًا) → drafts/ + Issue مراجعة                │
   └───────────────────────────────────────────────────────────────────┘

   ┌─ 5) مقال من موجز تحريري (Issue بوسم مقال) ─────────────────────────┐
   │  استخراج وقائع/رأي الموجز → تسمية أي حدث مُلمَّح إليه من نتائج       │
   │  البحث وحدها → مصدرَين مستقلَّين فأكثر لكل واقعة → عتبة كفاية →      │
   │  اختيار سؤال العنوان → صياغة مقال جديد (رأي الكاتب منسوب لا منسوخ)  │
   │  → drafts/ + Issue مراجعة                                          │
   └───────────────────────────────────────────────────────────────────┘
                          ↓  (كل المسارات تصل هنا)
   ┌─ النشر (بعد وسم approved على Issue المراجعة) ──────────────────────┐
   │  تعليم ✔️ على ما يعجبك → نشر مُتباعد الأوقات (لا فواصل منتظمة كي    │
   │  لا يبدو آليًا) عبر Graph API → تعليق برابط المصدر → إغلاق الـ Issue │
   └───────────────────────────────────────────────────────────────────┘
```

مسارات دعم مستقلة لا تولّد محتوى بنفسها: **استبدال صورة** مسودة موجودة بلا إعادة
صياغة، **تسجيل أسباب الرفض** وتغذيتها لفرز لاحق، **تقرير أداء أسبوعي** من Facebook
Insights يقترح تعديل أوزان `config.yaml`.

---

## الإعداد — 6 خطوات

### 1) أنشئ المستودع

```bash
git init
git add .
git commit -m "بوت الأخبار الرائجة"
git branch -M main
git remote add origin https://github.com/USERNAME/trendnews.git
git push -u origin main
```

> **المستودع العام أفضل**: معاينات الصور داخل الـ Issue تعمل مباشرة. في المستودع
> الخاص لن تظهر الصور مضمّنة، لكن البوت يرفعها كـ Artifact قابل للتنزيل ويضيف
> رابطها في نص الـ Issue.

### 2) مفتاح Claude API

من [console.anthropic.com](https://console.anthropic.com) → API Keys → Create Key.

### 3) صفحة فيسبوك وتوكن النشر

النشر التلقائي يعمل على **صفحة (Page)** فقط — لا على حساب شخصي.

1. أنشئ تطبيقًا على [developers.facebook.com](https://developers.facebook.com) بنوع
   **Business**، وأضف منتج **Facebook Login**.
2. افتح [Graph API Explorer](https://developers.facebook.com/tools/explorer/):
   اختر تطبيقك، ثم **User Token**، وأضف الصلاحيتين:
   `pages_manage_posts` و `pages_read_engagement`.
3. نفّذ `GET /me/accounts` → انسخ `id` صفحتك (هذا `FB_PAGE_ID`) و `access_token`.
4. **بدّل التوكن إلى طويل الأمد** — التوكن الأولي يعيش ساعتين فقط:

```bash
curl -G "https://graph.facebook.com/v21.0/oauth/access_token" \
  -d "grant_type=fb_exchange_token" \
  -d "client_id=APP_ID" \
  -d "client_secret=APP_SECRET" \
  -d "fb_exchange_token=SHORT_LIVED_PAGE_TOKEN"
```

الناتج هو `FB_PAGE_ACCESS_TOKEN` — توكن صفحة لا تنتهي صلاحيته عادةً، لكنه يُبطل
إذا غيّرت كلمة مرور فيسبوك أو ألغيت صلاحيات التطبيق.

### 4) أضف الأسرار على GitHub

`Settings → Secrets and variables → Actions → New repository secret`

| الاسم | القيمة |
|---|---|
| `ANTHROPIC_API_KEY` | `sk-ant-...` |
| `FB_PAGE_ID` | معرّف الصفحة الرقمي |
| `FB_PAGE_ACCESS_TOKEN` | التوكن طويل الأمد من الخطوة 3 |

`GITHUB_TOKEN` لا يحتاج إعدادًا — يوفّره GitHub تلقائيًا.

### 5) اسمح للـ Actions بالكتابة

`Settings → Actions → General → Workflow permissions`
→ اختر **Read and write permissions** ✔️
→ وفعّل **Allow GitHub Actions to create and approve pull requests** ✔️

بدون هذه الخطوة سيفشل البوت في رفع المسودات وفتح الـ Issues.

### 6) تحقّق قبل أول نشر حقيقي

`Actions → 2️⃣ نشر المعتمد على فيسبوك → Run workflow` مع تفعيل `verify_only ✔️`

إن ظهر اسم صفحتك وعدد متابعيها في السجل، فالتوكن سليم.

---

## الاستخدام اليومي

**التشغيل تلقائي** لا يعتمد على جدولة GitHub الداخلية (`schedule:` في
`collect.yml`، `radar.yml`، `queue.yml`) لأنها كانت تُسقط المواعيد بلا ضمان؛
بدلًا من ذلك تستدعي خدمة خارجية (cron-job.org) الـ `workflow_dispatch` مباشرة
عدة مرات يوميًا لكل من هذه الثلاثة (الجمع أقل تكرارًا، الرادار والطابور كل
~١٥ دقيقة). لتغيير المواعيد عدّل جدولة المهام في حساب cron-job.org، وليس ملفات
الـ workflow. الاستثناء الوحيد: `insights.yml` (التقرير الأسبوعي) لا يزال يعمل
بجدولة GitHub الأصلية (كل اثنين ٠٧:٠٠ UTC) لأن تكراره منخفض جدًا ليتأثر بالمشكلة.

**للتشغيل الفوري**: `Actions → 1️⃣ جمع الأخبار → Run workflow`

### دورة المراجعة (تشترك فيها كل المسارات الخمسة)

إن كان `preselect.enabled: true` (الافتراضي)، يفتح مسار الجمع أولًا **Issue
اختيار** بعناوين فقط (بلا صياغة ولا صورة بعد) — علّم ✔️ على ما تريد صياغته
كاملًا أو نشره فورًا، وعلى ما لا تريد اذكر سببًا اختياريًا، ثم أضف وسم
`approved`؛ عندها فقط تُصاغ وتُبنى الصورة لما اخترت. المسارات الأخرى (رادار،
طلب، تحقق، مقال) تصوغ مباشرة بلا مرحلة اختيار منفصلة.

1. يفتح البوت Issue بعنوان `📰 مسودات ...` فيه كل منشور مع صورته ونصه الكامل.
2. علّم ✔️ على المنشورات التي توافق عليها (مربعات الاختيار).
3. أضف الوسم `approved` إلى الـ Issue.
4. ينشر البوت المحدد فقط (بفواصل زمنية عشوائية، لا منتظمة، كي لا يبدو آليًا)،
   يعلّق بروابط المصدر تحت كل منشور، ويغلق الـ Issue.

**لتعديل نص منشور قبل النشر**: افتح ملف `drafts/التاريخ/المعرّف.json` على GitHub،
عدّل حقل `caption`، واحفظ (Commit). ثم اعتمد كالمعتاد.

**لتجاهل الدفعة كلها**: أغلق الـ Issue بلا وسم — الأسباب المكتوبة (إن وُجدت)
تُسجَّل وتُغذّي فرز المرات القادمة.

**لاستبدال صورة مسودة**: تحت كل منشور في الـ Issue مربعٌ وفراغ:

```
- [ ] 🖼️ استبدل الصورة بالرابط أدناه
    الرابط:
```

حرّر الـ Issue (زر ✏️)، الصق رابط صورة مباشر بعد كلمة «الرابط:» واحفظ، ثم
علّم المربع. يُعاد بناء البطاقة على صورتك خلال دقيقة، وتُحدَّث الصورة في
الـ Issue، ويُفرَّغ المربع تلقائيًا. النص لا يُعاد صياغته — الصورة وحدها
تتغير، فلا استدعاء للنموذج ولا تكلفة. البديل لمن يفضّله: تعليق فيه
`/صورة المعرّف الرابط`.

المسودات التي وصلت بلا صورة صالحة يعلوها تنبيه «بلا صورة للخبر»، وبطاقتها
مبنية على خلفية مصممة. الرابط يجب أن يكون لصورة مباشرة (تنتهي بـ `.jpg`
أو `.png`) لا لصفحة تحتوي صورة: في المتصفح زر أيمن على الصورة ← «نسخ عنوان
الصورة».

### رادار العاجل — نشر بلا مراجعة بشرط عتبات صارمة

يعمل تلقائيًا (`radar.yml`) بلا تدخل — يفحص تسارع تغطية المصادر لعيّنة من
المصادر الموثوقة بلا أي استدعاء نموذج، ولا يكلّف شيئًا إلا حين يتجاوز خبر عتبات
السرعة/الدرجة/عدد المصادر (`config.yaml: radar`). حتى عندها، النشر التلقائي
بلا مراجعة بشرية له عتبات أعلى بكثير ومنفصلة (`auto_publish_min_score`,
`auto_publish_min_sources`)، ويُرفض تمامًا لو: تجاوز السقف اليومي، المصدر رسمي/
حكومي وحيد، الخبر من فئة «خفيفة»، تعذّرت قراءة نص المصدر، أو تبيّن أنه تحديث
لخبر منشور سابقًا لا خبر جديد. أي خبر يجتاز الفحص الرخيص لكنه لا يجتاز عتبات
النشر التلقائي يُحفظ كمرشّح لدورة الاختيار العادية بدل إهدار العمل المُنجَز فيه.

**للتشغيل الفوري (فحص بلا نشر)**: `Actions → 🚨 رادار العاجل → Run workflow` مع
`dry_run ✔️`.

### لطلب خبر بنفسك (عكس الدورة العادية — أنت تحدد الموضوع لا الخلاصات)

- `Actions → طلب خبر بكلمات → Run workflow` واكتب الكلمات في الخانة.
- أو افتح Issue عنوانه هو الكلمات وضع عليه الوسم `طلب` — أسهل من الهاتف.

يبحث البوت في خلاصة بحث Google News بالعربية والإنجليزية معًا (بلا
مفتاح وبلا تكلفة)، ويصوغ مسودة من أقوى نتيجة ثم يعرضها للمراجعة كالمعتاد.
خيار `dry_run` يعرض نتائج البحث بلا صياغة — مجاني تمامًا، مفيد لتجربة
صياغة الكلمات قبل إنفاق استدعاء. الإعدادات في `config.yaml` تحت `request`:
نافذة البحث بالأيام، وعدد كلمات الطلب التي يجب أن تظهر في الخبر، ولغات البحث.

### للتحقق من مقال ملصق قبل الكتابة عنه (بحث وتقرير، ثم مسودة إن كفى المؤكَّد)

- افتح Issue جديد، الصق نص المقال في جسمه، وضع عليه الوسم `تحقق`.
- **المرحلة الأولى — التقرير:** يستخرج البوت وقائع المقال وأسئلته
  المفتوحة، يبحث عن كل واحدة في مصادر مستقلة عبر الآلية نفسها المستخدمة
  في «طلب خبر بكلمات»، ثم يعلّق على الـ Issue بتقرير: الموضوع، جدول
  الوقائع وحكمها (مؤكَّدة/مؤكَّدة مع اعتراض مصدر/شبه مؤكَّدة/مصدر واحد/لا
  مصدر/يخالفها مصدر) — بلوغ العدد الكافي من المصادر لا يكفي وحده
  لـ«مؤكَّدة»: يلزم مصدر معروف واحد على الأقل بينها (لا مجموع أوزان)، وإلا
  بقيت «شبه مؤكَّدة» رغم العدد. واقعة مرجعية لا تتعلق بدورة الأخبار
  الحالية (سنة صدور كتاب، تاريخ معاهدة قديمة...) تُبحث بلا قيد زمني، فلا
  يُقرأ "لا نتائج بحث" عليها خطأً كحكم نافٍ، الأسئلة وأجوبتها إن وُجدت، وقسم
  مُفرَد لما خالفت فيه المصادر رواية المقال، وحكمًا نهائيًا صريحًا: هل يكفي
  هذا لخبر قائم بذاته أم لا.
- **المرحلة الثانية — المسودة:** إن كفى المؤكَّد (واقعتان مستقلتان بمصدرين
  فأكثر لكل منهما على الأقل، والواقعة المحورية من بينهما — واقعة أيّدها
  العدد الكافي من المصادر لكن خالفها مصدر آخر تُصنَّف «مؤكَّدة مع اعتراض
  مصدر» لا «مؤكَّدة»، ولا تُحتسَب هنا)، يصوغ البوت مسودة منشور من هذا المؤكَّد
  وحده — **لا من المقال الملصق إطلاقًا**: دالّة الصياغة لا تستقبل نص المقال
  ولا عنوانه في أي حال، فلا يمكن أن تنتقل منه جملة، ولا أن يفرض زاويته أو
  نتيجته على المسودة حتى لو خالفتها المصادر. المسودة تدخل دورة المراجعة
  العادية (نفس ملف `drafts/<التاريخ>/…json`، نفس Issue المراجعة، نفس وسم
  `approved`) بلا مسار موازٍ، وتُعلَّم بحقل `origin: "verify"` معلوماتي بحت —
  بلا أي امتياز في المراجعة. فحص لفظي بعدي يرفض المسودة (بلا إعادة محاولة) إن
  حملت تتابع كلمات منسوخًا حرفيًا من المقال أو من مصدر مؤكِّد واحد بعينه؛
  تتابع وارد حرفيًا في مصدرين مستقلين مؤكِّدين فأكثر مستثنى (ليس نسخًا، بل
  صياغة الحدث نفسها كما تكرّرت في تغطيات مستقلة)، وكذلك اقتباس منسوب موجود
  حرفيًا في مصدر مؤكِّد. لم يكفِ المؤكَّد، أو رفض النموذج الخبر تحريريًا
  (`newsworthy=false`)؟ لا مسودة — والتقرير يذكر السبب المحدد. الإعدادات في
  `config.yaml` تحت `verify` (المرحلة الأولى) و`verify_draft` (المرحلة
  الثانية)، والكود في `src/verify.py` و`src/verify_draft.py`.

### لكتابة مقال من فكرتك (موجز تحريري، لا مقال جاهز للتحقق منه)

- افتح Issue جديد، اكتب فكرتك ومعلوماتك ورأيك في جسمه (هذا **موجز**، وليس
  نصًا جاهزًا يُحقَّق منه حرفيًا كما في مسار «تحقق»)، وضع عليه الوسم `مقال`.
- يستخرج البوت من الموجز وقائع ورأيك المُعبَّر عنه، وإن أشرت لحدث بلا تسميته
  صراحة (مثل «الاجتماع الأخير») يحاول تسميته من نتائج بحث مستقلة فقط — أبدًا
  من معرفة النموذج المسبقة. كل واقعة، سواء سُمّيت في موجزك أو اكتُشف اسمها،
  تحتاج مصدرَين مستقلَّين فأكثر (`article.min_confirm_sources`) قبل اعتمادها،
  وإلا سقطت وذُكر السبب في التقرير — لا تُحذف بصمت. إن جمع الموجز عددًا كافيًا
  من الوقائع المصدَّقة (`article.min_grounded_facts`)، يختار البوت سؤال
  العنوان من بينها ويصوغ مقالًا جديدًا، يُدمج فيه رأيك منسوبًا صراحةً
  («ويرى كاتب الموجز أن...») لا منسوخًا، ما لم تُعطِّل هذا بـ
  `article.include_opinion: false`. فحص أصالة لفظي (نفس آلية مسار «تحقق»)
  يرفض أي عبارة منسوخة حرفيًا من موجزك أو من مصدر واحد بعينه. المسودة تدخل
  دورة المراجعة العادية وتُعلَّم بحقل `origin: "article"`. الإعدادات في
  `config.yaml` تحت `article`، والكود في `src/article.py`.

---

## التخصيص — `config.yaml`

كل شيء يُضبط من ملف واحد بلا لمس الكود — أكثر من ٢٠٠ مفتاح موزّعة على أقسام
`brand` (المصادر ٩٤ خلاصة)، `trends`، `selection`، `preselect`، `writer`،
`request`، `verify`، `verify_draft`، `article`، `radar`، `merge`، `screening`،
`reading`، `analysis`، `image`، `reel`، `image_search`، `facebook`. أبرزها:

| الإعداد | ماذا يفعل |
|---|---|
| `brand.name` / `handle` | الاسم والمعرّف على الصورة |
| `brand.primary_color` / `accent_color` | لون الشريط ولون التمييز |
| `sources` | أضف/احذف خلاصات RSS، وحدّد `region` و `weight` و `state_media` لكل مصدر |
| `preselect.enabled` | `true` يفتح Issue اختيار بعناوين فقط قبل الصياغة (توفير تكلفة)؛ `false` يعود للمسار القديم (صياغة الدفعة كاملة) |
| `selection.drafts_per_run` | عدد المسودات في كل تشغيل |
| `selection.min_sources_for_trend` | ارفعه لنشر ما تتفق عليه عدة مصادر فقط |
| `selection.max_age_hours` | أقصى عمر مقبول للخبر |
| `selection.dedupe_memory_days` | مدة منع إعادة نشر خبر مشابه |
| `selection.blocklist_keywords` | كلمات تستبعد الخبر تمامًا |
| `writer.model` | `claude-sonnet-5` للجودة، أو `claude-haiku-4-5-20251001` للتوفير |
| `writer.tone` | نبرة الكتابة العربية |
| `radar.auto_publish` | `false` يعطّل النشر بلا مراجعة نهائيًا (يبقى الفحص/الصياغة فعّالين) |
| `radar.auto_publish_daily_limit` | سقف عدد المنشورات المنشورة تلقائيًا يوميًا |
| `verify.min_confirm_sources` / `article.min_confirm_sources` | عدد المصادر المستقلة المطلوب لاعتبار واقعة «مؤكَّدة» |
| `article.include_opinion` | إدراج رأي كاتب الموجز في المسودة منسوبًا أم لا |
| `image_search.providers` | مصادر الصور الاحتياطية عند غياب صورة من الناشر (ويكيميديا/Openverse فقط) |
| `facebook.schedule_mode` | `burst` (فاصل عشوائي بين المنشورات) أو أنماط أخرى |

### إضافة مصدر جديد

```yaml
  - name: "Sky News Arabia"
    url: "https://www.skynewsarabia.com/rss"
    region: "mena"
    weight: 1.0
```

`region` يُستخدم للتنوع الجغرافي (سقفه `max_per_region`)، و `weight` يرفع أو
يخفض أولوية المصدر في مؤشر الترند. `state_media: true` (اختياري) يخفض الدرجة
قليلًا ويظهر تنبيهًا في الـ Issue، دون استبعاد المصدر.

---

## التشغيل المحلي

```bash
pip install -r requirements.txt
bash scripts/fetch_fonts.sh          # الخطوط مضمَّنة، هذا للتحديث فقط
cp .env.example .env                 # ضع مفاتيحك فيه
set -a && source .env && set +a

python -m src.collect --limit 2      # توليد مسودتين للتجربة
python -m src.publish --verify       # فحص توكن فيسبوك
python -m src.publish --all-pending  # نشر كل المعلّق (احذر: نشر حقيقي)
```

### الاختبارات

```bash
python -m tests.test_pipeline
```

٨٥ دالة اختبار (نحو ١٢٠٠ حالة تحقق فردية) تغطي: التشابه والدمج (حرفي عبر
Jaccard ودلالي عبر لغات) والترتيب، منع التكرار، تشكيل العربية وتقسيم
الأسطر، الأنبوب الكامل بمساريه (الصياغة المباشرة والاختيار المسبق)، دورة
المراجعة، رادار العاجل وعتباته، مسار الطلب، مساري التحقق والمقال بكل حراسهما،
الجدولة والنشر المتباعد، والتقرير الأسبوعي — كلها بمحاكاة الشبكة و Claude API
(`install_fakes()`)، بلا أي طلب خارجي وبلا تكلفة. هذا الملف الوحيد هو بوابة
الجودة في المشروع: لا pytest ولا أداة تحقق أسلوب (linter) منفصلة.

---

## بنية المشروع

```
├── config.yaml                 ← كل الإعدادات (٩٤ مصدرًا + كل العتبات)
├── src/
│   ├── config.py                تحميل config.yaml + مسارات drafts/state القابلة للتحويل في الاختبارات
│   ├── store.py                 حفظ المسودات/المرشّحين + ذاكرة منع التكرار
│   │
│   ├── sources.py                جلب RSS + فك روابط Google News + استخراج الصور
│   ├── rank.py                   دمج المكرر (جاكارد) + مؤشر الترند + تنوع جغرافي
│   ├── merge.py                  دمج دلالي عبر اللغات (نفس الحدث بلغات مختلفة)
│   ├── trends.py / velocity.py   إشارتا ترتيب: بحث جوجل الرائج + تسارع تغطية المصادر
│   ├── screen.py                 فرز رخيص (Haiku) قبل أي خطوة مكلفة
│   ├── preselect.py               بيانات/نص Issue اختيار العناوين قبل الصياغة
│   ├── extract.py                 استخراج نص المقال من عدة مصادر للتحليل المقارن
│   ├── writer.py                  صياغة عربية عبر Claude API
│   ├── imaging.py                 بناء الكارت: قص + تعتيم + عنوان عربي RTL + دمج صورتين
│   ├── vision.py                  اختيار تخطيط الصورتين (كشف أجسام/وجوه، اختياري)
│   ├── imagesearch.py             صورة بديلة من ويكيميديا/Openverse عند غياب صورة الناشر
│   ├── reel.py                    فيديو عمودي بديل من نفس الصورة والعنوان (ffmpeg محلي)
│   ├── collect.py                 الأنبوب الرئيسي (جمع → ترتيب → فرز → صياغة أو اختيار)
│   ├── collect_finalize.py        صياغة/نشر ما اخترته بعد Issue الاختيار
│   ├── open_review.py             فتح Issue المراجعة أو الاختيار
│   ├── review.py                  بناء/قراءة Issue المراجعة (مربعات، وسوم)
│   ├── publish.py                 نشر المعتمد (فوري/متباعد/طابور)
│   ├── schedule.py                حساب فواصل النشر العشوائية
│   ├── facebook.py                غلاف Graph API (نشر، تعليق، مقاييس)
│   ├── setimage.py                استبدال صورة مسودة بلا إعادة صياغة
│   ├── feedback.py / collect_feedback.py   تسجيل أسباب الرفض وتغذيتها للفرز
│   ├── insights.py                تقرير أداء أسبوعي واقتراحات لضبط config.yaml
│   ├── radar.py                   رادار العاجل + عتبات النشر بلا مراجعة
│   ├── request.py                 مسار «اطلب خبرًا بكلمات»
│   ├── evidence.py                محرك بحث/قراءة مشترك بين verify.py و article.py
│   ├── verify.py                  تحقق من مقال ملصق (استخراج → بحث → حكم → تقرير)
│   ├── verify_draft.py            صياغة مسودة من الوقائع المؤكَّدة فقط (لا من المقال)
│   ├── article.py                 مقال جديد من موجز تحريري (وقائع مصدَّقة + رأي منسوب)
│   └── check_sources.py           تشخيص يدوي: أي خلاصات RSS ما زالت حيّة
│
├── .github/workflows/           ١٢ ملف: collect / publish / radar / queue / request /
│                                 verify / article / feedback / image / insights / claude / static
├── assets/fonts/                 عدة خطوط عربية (Tajawal وغيره، رخصة OFL، مضمَّنة)
├── drafts/                       المسودات: json + jpg لكل منشور
└── state/                        history.json (منع التكرار) + radar.json + candidates/
```

---

## تفاصيل تقنية تستحق الانتباه

**النص العربي على الصورة** — يحتاج `arabic-reshaper` لوصل الحروف و `python-bidi`
لاتجاه الكتابة (أو تشكيل RTL أصلي عبر Raqm إن توفّر). المهم: تقسيم الأسطر يجري
على النص المنطقي **قبل** التشكيل، وإلا انكسرت الكلمات وظهرت الحروف منفصلة.

**ترتيب خطوات الـ workflow** — الصور تُرفع للمستودع **قبل** فتح الـ Issue، وإلا
ظهرت المعاينات كـ 404 لأن روابط `raw.githubusercontent` لم توجد بعد.

**ترتيب التحقق قبل اختيار السؤال (مسار «مقال»)** — تصفية الوقائع بعدد المصادر
تجري **قبل** اختيار الواقعة المحورية للعنوان، لا العكس؛ وإلا قد يُختار عنوان
حول واقعة لم تُتحقَّق فعليًا لمجرد أنها اختيرت أولًا.

**القص** — يميل للثلث الأعلى من الصورة لأن الوجوه غالبًا هناك، مع تدرّج تعتيم
أسّي (لا خطّي) يضمن قراءة النص فوق أي صورة بأي سطوع.

**البرومبت** — يمنع اختراع الأرقام والتصريحات، يمنع النسخ الحرفي من المصدر، ويعيد
`newsworthy: false` للأخبار التافهة أو الترويجية فيتخطّاها البوت تلقائيًا.

**مؤشر الترند** — `3×log₂(عدد المصادر) + 2×log₂(عدد المناطق) + 1.5×وزن المصدر +
2×الحداثة + وزن الترند×تطابق بحث جوجل + وزن السرعة×تسارع المصادر + 0.8 إن وُجدت
صورة`. اللوغاريتم يمنع سيطرة خبر واحد تغطيه عشرات الوكالات.

**لا `temperature` أبدًا** — النماذج المستخدمة هنا ترفضه بخطأ 400؛ اختبار ثابت
(`test_no_temperature_param`) يفشل الحزمة كلها لو ظهر مجددًا في أي استدعاء نموذج.

---

## حل المشاكل

| المشكلة | السبب والحل |
|---|---|
| فشل رفع المسودات | فعّل **Read and write permissions** (الخطوة 5) |
| الصور لا تظهر في الـ Issue | مستودع خاص — نزّلها من Artifacts في صفحة التشغيل |
| `خطأ فيسبوك [190]` | التوكن انتهى — أعد توليده كتوكن طويل الأمد |
| `خطأ فيسبوك [200]` | صلاحيات ناقصة — أضف `pages_manage_posts` |
| مربعات مربعة بدل الحروف | الخطوط مفقودة — شغّل `bash scripts/fetch_fonts.sh` |
| `لم يُجلب أي خبر` | مصدر RSS تغيّر رابطه — تحقّق من `sources` في `config.yaml`، أو شغّل `python -m src.check_sources` لفحص كل الخلاصات دفعة واحدة |
| مسودات قليلة جدًا | خفّض `min_sources_for_trend` أو ارفع `max_age_hours` |
| Issue اختيار بدل Issue مراجعة عادي | `preselect.enabled: true` — هذا متوقَّع؛ عطّله لو تفضّل الصياغة المباشرة |
| مسار «تحقق»/«مقال» لا يصوغ رغم تقرير موجب | تحقّق أن الـ workflow يحمل `VERIFY_DRAFT_WRITE_ENABLED=true`، وأن مُضيف الوسم له صلاحية كتابة على المستودع |

---

## ملاحظتان مهمتان

**سياسة فيسبوك**: النشر الآلي المكثف لأخبار منسوخة قد يُخفّض وصول الصفحة أو
يعرّضها للتقييد. البقاء عند عدد منشورات معقول يوميًا مع صياغة أصلية (وهو ما
يفرضه البرومبت) وفواصل زمنية غير منتظمة (`schedule.py`) أكثر أمانًا من رفع
العدد.

**الدقة الصحفية**: النموذج قد يخطئ في نقل تفصيل أو اسم. مسار المراجعة موجود
لهذا السبب تحديدًا — اقرأ النص قبل الاعتماد، خاصة في الأخبار الحساسة سياسيًا.
مساريّ «تحقق» و«مقال» يقلّصان هذا الخطر ببحث مستقل قبل الصياغة، لكنهما لا
يلغيانه: راجع التقرير المرفق دائمًا.

---

## الترخيص والحقوق

الكود لك. الخطوط في `assets/fonts/` مرخّصة تحت
[SIL Open Font License](https://scripts.sil.org/OFL) وتسمح بالاستخدام التجاري.
احترم حقوق الناشرين: البوت يذكر المصدر ويضع رابطه في كل منشور، ولا ينسخ نصوصهم
حرفيًا.
