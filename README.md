# 📰 بوت الأخبار الرائجة → فيسبوك

يستكشف الأخبار الرائجة عالميًا، يصوغها بالعربية، يبني لها صورة منشور من صورة الخبر
الأصلية، ثم يعرضها عليك للمراجعة — وينشر ما توافق عليه فقط على صفحة فيسبوك.

يعمل بالكامل على **GitHub Actions** المجاني، بلا سيرفر وبلا تكلفة استضافة.

---

## كيف يعمل؟

```
        ┌─ مسار الجمع (تلقائي كل 6 ساعات) ────────────────────┐
        │                                                      │
   9 خلاصات RSS  →  دمج المكرر  →  ترتيب بمؤشر الترند         │
   (مجانية بلا      (تشابه          (تغطية المصادر +           │
    مفاتيح)          جاكارد)         تنوع جغرافي + حداثة)      │
                          ↓                                    │
             فلترة ما نُشر سابقًا (ذاكرة 5 أيام)                │
                          ↓                                    │
             صياغة عربية عبر Claude API                        │
                          ↓                                    │
             صورة الخبر الأصلية + إطار + عنوان عربي            │
                          ↓                                    │
             مسودات في drafts/  +  Issue للمراجعة              │
        └──────────────────────────────────────────────────────┘
                          ↓
        ┌─ مسار النشر (بعد موافقتك) ───────────────────────────┐
        │  تعلّم ✔️ على ما يعجبك  →  تضيف وسم `approved`       │
        │              ↓                                        │
        │  نشر على الصفحة عبر Graph API  →  تعليق بالروابط     │
        └───────────────────────────────────────────────────────┘
```

---

## الإعداد — 6 خطوات

### 1) أنشئ المستودع

```bash
git init
git add .
git commit -m "بوت الأخبار الرائجة"
git branch -M main
git remote add origin https://github.com/USERNAME/trendnews.git
git push -u origin main
```

> **المستودع العام أفضل**: معاينات الصور داخل الـ Issue تعمل مباشرة. في المستودع
> الخاص لن تظهر الصور مضمّنة، لكن البوت يرفعها كـ Artifact قابل للتنزيل ويضيف
> رابطها في نص الـ Issue.

### 2) مفتاح Claude API

من [console.anthropic.com](https://console.anthropic.com) → API Keys → Create Key.

### 3) صفحة فيسبوك وتوكن النشر

النشر التلقائي يعمل على **صفحة (Page)** فقط — لا على حساب شخصي.

1. أنشئ تطبيقًا على [developers.facebook.com](https://developers.facebook.com) بنوع
   **Business**، وأضف منتج **Facebook Login**.
2. افتح [Graph API Explorer](https://developers.facebook.com/tools/explorer/):
   اختر تطبيقك، ثم **User Token**، وأضف الصلاحيتين:
   `pages_manage_posts` و `pages_read_engagement`.
3. نفّذ `GET /me/accounts` → انسخ `id` صفحتك (هذا `FB_PAGE_ID`) و `access_token`.
4. **بدّل التوكن إلى طويل الأمد** — التوكن الأولي يعيش ساعتين فقط:

```bash
curl -G "https://graph.facebook.com/v21.0/oauth/access_token" \
  -d "grant_type=fb_exchange_token" \
  -d "client_id=APP_ID" \
  -d "client_secret=APP_SECRET" \
  -d "fb_exchange_token=SHORT_LIVED_PAGE_TOKEN"
```

الناتج هو `FB_PAGE_ACCESS_TOKEN` — توكن صفحة لا تنتهي صلاحيته عادةً، لكنه يُبطل
إذا غيّرت كلمة مرور فيسبوك أو ألغيت صلاحيات التطبيق.

### 4) أضف الأسرار على GitHub

`Settings → Secrets and variables → Actions → New repository secret`

| الاسم | القيمة |
|---|---|
| `ANTHROPIC_API_KEY` | `sk-ant-...` |
| `FB_PAGE_ID` | معرّف الصفحة الرقمي |
| `FB_PAGE_ACCESS_TOKEN` | التوكن طويل الأمد من الخطوة 3 |

`GITHUB_TOKEN` لا يحتاج إعدادًا — يوفّره GitHub تلقائيًا.

### 5) اسمح للـ Actions بالكتابة

`Settings → Actions → General → Workflow permissions`
→ اختر **Read and write permissions** ✔️
→ وفعّل **Allow GitHub Actions to create and approve pull requests** ✔️

بدون هذه الخطوة سيفشل البوت في رفع المسودات وفتح الـ Issues.

### 6) تحقّق قبل أول نشر حقيقي

`Actions → 2️⃣ نشر المعتمد على فيسبوك → Run workflow` مع تفعيل `verify_only ✔️`

إن ظهر اسم صفحتك وعدد متابعيها في السجل، فالتوكن سليم.

---

## الاستخدام اليومي

**التشغيل تلقائي** لا يعتمد على جدولة GitHub الداخلية (`schedule:` في
`collect.yml`) لأنها كانت تُسقط المواعيد بلا ضمان؛ بدلًا من ذلك تستدعي خدمة
خارجية (cron-job.org) الـ `workflow_dispatch` مباشرة عدة مرات يوميًا
(01 · 06 · 08 · 10 · 12 · 14 · 16 · 18 · 19 · 20 · 21 · 22 · 23 بتوقيت
إسطنبول). لتغيير المواعيد عدّل جدولة المهمة في حساب cron-job.org، وليس ملف
`collect.yml`.

**للتشغيل الفوري**: `Actions → 1️⃣ جمع الأخبار → Run workflow`

**دورة المراجعة**:

1. يفتح البوت Issue بعنوان `📰 مسودات ...` فيه كل منشور مع صورته ونصه الكامل.
2. علّم ✔️ على المنشورات التي توافق عليها (مربعات الاختيار).
3. أضف الوسم `approved` إلى الـ Issue.
4. ينشر البوت المحدد فقط، يعلّق بروابط المنشورات، ويغلق الـ Issue.

**لتعديل نص منشور قبل النشر**: افتح ملف `drafts/التاريخ/المعرّف.json` على GitHub،
عدّل حقل `caption`، واحفظ (Commit). ثم اعتمد كالمعتاد.

**لتجاهل الدفعة كلها**: أغلق الـ Issue بلا وسم.

**لاستبدال صورة مسودة**: تحت كل منشور في الـ Issue مربعٌ وفراغ:

```
- [ ] 🖼️ استبدل الصورة بالرابط أدناه
    الرابط:
```

حرّر الـ Issue (زر ✏️)، الصق رابط صورة مباشر بعد كلمة «الرابط:» واحفظ، ثم
علّم المربع. يُعاد بناء البطاقة على صورتك خلال دقيقة، وتُحدَّث الصورة في
الـ Issue، ويُفرَّغ المربع تلقائيًا. النص لا يُعاد صياغته — الصورة وحدها
تتغير، فلا استدعاء للنموذج ولا تكلفة.

المسودات التي وصلت بلا صورة صالحة يعلوها تنبيه «بلا صورة للخبر»، وبطاقتها
مبنية على خلفية مصممة. الرابط يجب أن يكون لصورة مباشرة (تنتهي بـ `.jpg`
أو `.png`) لا لصفحة تحتوي صورة: في المتصفح زر أيمن على الصورة ← «نسخ عنوان
الصورة». البديل لمن يفضّله: تعليق فيه `/صورة المعرّف الرابط`.

**لطلب خبر بنفسك** (عكس الدورة العادية — أنت تحدد الموضوع لا الخلاصات):

- `Actions → طلب خبر بكلمات → Run workflow` واكتب الكلمات في الخانة.
- أو افتح Issue عنوانه هو الكلمات وضع عليه الوسم `طلب` — أسهل من الهاتف.

يبحث البوت في خلاصة بحث Google News بالعربية والإنجليزية معًا (بلا
مفتاح وبلا تكلفة)، ويصوغ مسودة من أقوى نتيجة ثم يعرضها للمراجعة كالمعتاد.
خيار `dry_run` يعرض نتائج البحث بلا صياغة — مجاني تمامًا، مفيد لتجربة
صياغة الكلمات قبل إنفاق استدعاء. الإعدادات في `config.yaml` تحت `request`:
نافذة البحث بالأيام، وعدد كلمات الطلب التي يجب أن تظهر في الخبر، ولغات البحث.

**للتحقق من مقال ملصق قبل الكتابة عنه** (بحث وتقرير، ثم مسودة إن كفى المؤكَّد):

- افتح Issue جديد، الصق نص المقال في جسمه، وضع عليه الوسم `تحقق`.
- **المرحلة الأولى — التقرير:** يستخرج البوت وقائع المقال وأسئلته
  المفتوحة، يبحث عن كل واحدة في مصادر مستقلة عبر الآلية نفسها المستخدمة
  في «طلب خبر بكلمات»، ثم يعلّق على الـ Issue بتقرير: الموضوع، جدول
  الوقائع وحكمها (مؤكَّدة/شبه مؤكَّدة/مصدر واحد/لا مصدر/يخالفها مصدر)،
  الأسئلة وأجوبتها إن وُجدت، وقسم مُفرَد لما خالفت فيه المصادر رواية
  المقال، وحكمًا نهائيًا صريحًا: هل يكفي هذا لخبر قائم بذاته أم لا.
- **المرحلة الثانية — المسودة:** إن كفى المؤكَّد (واقعتان مستقلتان بمصدرين
  فأكثر لكل منهما على الأقل، والواقعة المحورية من بينهما)، يصوغ البوت
  مسودة منشور من هذا المؤكَّد وحده — **لا من المقال الملصق إطلاقًا**: دالّة
  الصياغة لا تستقبل نص المقال ولا عنوانه في أي حال، فلا يمكن أن تنتقل منه
  جملة، ولا أن يفرض زاويته أو نتيجته على المسودة حتى لو خالفتها المصادر.
  المسودة تدخل دورة المراجعة العادية (نفس ملف `drafts/<التاريخ>/…json`،
  نفس Issue المراجعة، نفس وسم `approved`) بلا مسار موازٍ، وتُعلَّم
  بحقل `origin: "verify"` معلوماتي بحت — بلا أي امتياز في المراجعة.
  فحص لفظي بعدي يرفض المسودة (بلا إعادة محاولة) إن حملت تتابع كلمات
  منسوخًا حرفيًا من المقال أو من مقتطفات المصادر؛ اقتباس منسوب موجود
  حرفيًا في مصدر مؤكِّد مستثنى من هذا الفحص. لم يكفِ المؤكَّد، أو رفض
  النموذج الخبر تحريريًا (`newsworthy=false`)؟ لا مسودة — والتقرير يذكر
  السبب المحدد. الإعدادات في `config.yaml` تحت `verify` (المرحلة الأولى)
  و`verify_draft` (المرحلة الثانية)، والكود في `src/verify.py` و
  `src/verify_draft.py`.

---

## التخصيص — `config.yaml`

كل شيء يُضبط من ملف واحد بلا لمس الكود:

| الإعداد | ماذا يفعل |
|---|---|
| `brand.name` / `handle` | الاسم والمعرّف على الصورة |
| `brand.primary_color` / `accent_color` | لون الشريط ولون التمييز |
| `sources` | أضف/احذف خلاصات RSS، وحدّد `region` و `weight` لكل مصدر |
| `selection.drafts_per_run` | عدد المسودات في كل تشغيل |
| `selection.min_sources_for_trend` | ارفعه إلى `2` لنشر ما تتفق عليه عدة مصادر فقط |
| `selection.max_age_hours` | أقصى عمر مقبول للخبر |
| `selection.dedupe_memory_days` | مدة منع إعادة نشر خبر مشابه |
| `selection.blocklist_keywords` | كلمات تستبعد الخبر تمامًا |
| `writer.model` | `claude-sonnet-5` للجودة، أو `claude-haiku-4-5-20251001` للتوفير |
| `writer.tone` | نبرة الكتابة العربية |
| `image.blur_background` | تمويه خفيف للخلفية |
| `facebook.publish_as_photo` | `true` منشور صورة، `false` منشور رابط |

### إضافة مصدر جديد

```yaml
  - name: "Sky News Arabia"
    url: "https://www.skynewsarabia.com/rss"
    region: "mena"
    weight: 1.0
```

`region` يُستخدم للتنوع الجغرافي (سقفه `max_per_region`)، و `weight` يرفع أو
يخفض أولوية المصدر في مؤشر الترند.

---

## التشغيل المحلي

```bash
pip install -r requirements.txt
bash scripts/fetch_fonts.sh          # الخطوط مضمَّنة، هذا للتحديث فقط
cp .env.example .env                 # ضع مفاتيحك فيه
set -a && source .env && set +a

python -m src.collect --limit 2      # توليد مسودتين للتجربة
python -m src.publish --verify       # فحص توكن فيسبوك
python -m src.publish --all-pending  # نشر كل المعلّق (احذر: نشر حقيقي)
```

### الاختبارات

```bash
python -m tests.test_pipeline
```

39 اختبارًا يغطي التشابه والدمج والترتيب، منع التكرار، تشكيل العربية وتقسيم
الأسطر، الأنبوب الكامل، ودورة المراجعة — كلها بمحاكاة الشبكة و Claude API،
بلا أي طلب خارجي وبلا تكلفة.

---

## بنية المشروع

```
├── config.yaml               ← كل الإعدادات
├── src/
│   ├── sources.py            جلب RSS + استخراج الصور (media:content ثم og:image)
│   ├── rank.py               دمج المكرر بتشابه جاكارد + مؤشر الترند
│   ├── writer.py             صياغة عربية عبر Claude API
│   ├── imaging.py            بناء الكارت: قص + تعتيم + عنوان عربي RTL
│   ├── facebook.py           النشر عبر Graph API
│   ├── store.py              المسودات + ذاكرة منع التكرار
│   ├── review.py             Issue المراجعة (بناء + قراءة العلامات)
│   ├── collect.py            الأنبوب الرئيسي
│   ├── open_review.py        فتح Issue المراجعة
│   └── publish.py            نشر المعتمد
├── .github/workflows/
│   ├── collect.yml           الجمع المجدول
│   └── publish.yml           النشر عند وسم approved
├── assets/fonts/             Tajawal (رخصة OFL، مضمَّن)
├── drafts/                   المسودات: json + jpg لكل منشور
└── state/history.json        ذاكرة منع التكرار
```

---

## تفاصيل تقنية تستحق الانتباه

**النص العربي على الصورة** — يحتاج `arabic-reshaper` لوصل الحروف و `python-bidi`
لاتجاه الكتابة. المهم: تقسيم الأسطر يجري على النص المنطقي **قبل** التشكيل، وإلا
انكسرت الكلمات وظهرت الحروف منفصلة.

**ترتيب خطوات الـ workflow** — الصور تُرفع للمستودع **قبل** فتح الـ Issue، وإلا
ظهرت المعاينات كـ 404 لأن روابط `raw.githubusercontent` لم توجد بعد.

**القص** — يميل للثلث الأعلى من الصورة لأن الوجوه غالبًا هناك، مع تدرّج تعتيم
أسّي (لا خطّي) يضمن قراءة النص فوق أي صورة بأي سطوع.

**البرومبت** — يمنع اختراع الأرقام والتصريحات، يمنع النسخ الحرفي من المصدر، ويعيد
`newsworthy: false` للأخبار التافهة أو الترويجية فيتخطّاها البوت تلقائيًا.

**مؤشر الترند** — `3×log₂(عدد المصادر) + 2×log₂(عدد المناطق) + 1.5×وزن المصدر +
2×الحداثة + 0.8 إن وُجدت صورة`. اللوغاريتم يمنع سيطرة خبر واحد تغطيه 20 وكالة.

---

## حل المشاكل

| المشكلة | السبب والحل |
|---|---|
| فشل رفع المسودات | فعّل **Read and write permissions** (الخطوة 5) |
| الصور لا تظهر في الـ Issue | مستودع خاص — نزّلها من Artifacts في صفحة التشغيل |
| `خطأ فيسبوك [190]` | التوكن انتهى — أعد توليده كتوكن طويل الأمد |
| `خطأ فيسبوك [200]` | صلاحيات ناقصة — أضف `pages_manage_posts` |
| مربعات مربعة بدل الحروف | الخطوط مفقودة — شغّل `bash scripts/fetch_fonts.sh` |
| `لم يُجلب أي خبر` | مصدر RSS تغيّر رابطه — تحقّق من `sources` في `config.yaml` |
| مسودات قليلة جدًا | خفّض `min_sources_for_trend` أو ارفع `max_age_hours` |

---

## ملاحظتان مهمتان

**سياسة فيسبوك**: النشر الآلي المكثف لأخبار منسوخة قد يُخفّض وصول الصفحة أو
يعرّضها للتقييد. البقاء عند 3–5 منشورات يوميًا مع صياغة أصلية (وهو ما يفرضه
البرومبت) أكثر أمانًا من رفع العدد.

**الدقة الصحفية**: النموذج قد يخطئ في نقل تفصيل أو اسم. مسار المراجعة موجود
لهذا السبب تحديدًا — اقرأ النص قبل الاعتماد، خاصة في الأخبار الحساسة سياسيًا.

---

## الترخيص والحقوق

الكود لك. خط **Tajawal** مرخّص تحت
[SIL Open Font License](https://scripts.sil.org/OFL) ويسمح بالاستخدام التجاري.
احترم حقوق الناشرين: البوت يذكر المصدر ويضع رابطه في كل منشور، ولا ينسخ نصوصهم
حرفيًا.
